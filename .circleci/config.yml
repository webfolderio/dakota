version: 2
jobs:
  build:
    
    working_directory: ~/dako

    docker:
      - image: circleci/openjdk:8-jdk-browsers

    steps:

      - checkout

      - restore_cache:
          key: dakota-{{ checksum "pom.xml" }}
      
      - run: mvn dependency:go-offline
      
      - save_cache:
          paths:
            - ~/.m2
          key: dakota-{{ checksum "pom.xml" }}
            
      - run: mvn package
      
      - store_test_results:
          path: target/surefire-reports
      
      - store_artifacts:
          path: target/dakota-1.0.0-SNAPSHOT.jar
