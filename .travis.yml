matrix:
  include:
   - os: osx
     osx_image: xcode10.1

cache:
  directories:
  - $HOME/.m2
  - $HOME/vcpkg-cache

install:
-|
  if [ ! -d "$HOME/vcpkg-cache" ]; then
   brew install gcc@8
 fi
 git clone https://github.com/Microsoft/vcpkg.git
 cd vcpkg
 if [ -d "$HOME/vcpkg-cache" ]; then
   cp $HOME/vcpkg-cache/vcpkg .
 else
   CC=gcc-8 CXX=g++-8 ./bootstrap-vcpkg.sh
   ./vcpkg integrate install
  fi
  ./vcpkg install restinio:x64-osx

script:
  - cd /home/travis/build/webfolderio/dakota/native && mkdir build
  - cd /home/travis/build/webfolderio/dakota/native/build && CC=clang CXX=clang++ cmake .. -DCMAKE_TOOLCHAIN_FILE=$HOME/vcpkg/scripts/buildsystems/vcpkg.cmake -DVCPKG_TARGET_TRIPLET=x64-osx
  - cd /home/travis/build/webfolderio/dakota/native/build && CC=clang CXX=clang++ cmake .. -DCMAKE_BUILD_TYPE=Release
  - cd /home/travis/build/webfolderio/dakota/native/build && CC=clang CXX=clang++ make
  - cd /home/travis/build/webfolderio/dakota/native/build && cp libdakota.dylib ../../src/main/resources/META-INF
  - cd /home/travis/build/webfolderio/dakota && mvn -q -DskipTests package
  - cd /home/travis/build/webfolderio/dakota && mvn test
  
